{
  "name": "serverest-cypress-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Automação Cypress (E2E + API) para ServeRest",
  "scripts": {
    "test": "cypress run",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "cypress": "^15.12.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
